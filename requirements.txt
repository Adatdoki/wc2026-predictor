# A predikciós motor és a kalibráció TISZTA PYTHON — semmi külső függőség.
# Ez csak a webfelülethez kell:
fastapi>=0.100
uvicorn>=0.23

# Termuxon, ha a pydantic v2 Rust-magja nem fordul:
#   pip install "fastapi==0.99.1" "pydantic<2" "uvicorn==0.22.0"

# Opcionális: a gpt_updater.py --interpret (valós hírcikkek értelmezése) használja.
# A rendszer enélkül is teljesen működik.
# openai>=1.0
